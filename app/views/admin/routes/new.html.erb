<h1>Новый маршрут</h1>

<%= form_for([:admin, @route]) do |form| %>
    <% if @route.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@route.errors.count, "error") %> prohibited this route from being saved:</h2>

          <ul>
            <% @route.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div>
      <%= form.collection_select(:station_last, RailwayStation.all, :id, :title) %>
    </div>

    <div>
      <%= form.collection_select(:station_first, RailwayStation.all, :id, :title) %>
    </div>

    <div class="actions">
      <%= form.submit 'Создать' %>
    </div>
<% end %>


<%= link_to 'Назад', admin_routes_path %>
